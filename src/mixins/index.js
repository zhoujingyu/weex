export default {
    methods: {
        // jump (to) {
        //     if (this.$router) {
        //         this.$router.push(to)
        //     }
        // },
        // replaceWith(replace){
        //     if (this.$router) {
        //         this.$router.replace(replace)
        //     }
        // }
    }
}
